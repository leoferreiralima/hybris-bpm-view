/**
 * BPMN-specific behavior for creating participants.
 */
declare function CreateParticipantBehavior(canvas: any, eventBus: any, modeling: any): void;
declare namespace CreateParticipantBehavior {
    const $inject: string[];
}
export default CreateParticipantBehavior;
export var PARTICIPANT_BORDER_WIDTH: number;
