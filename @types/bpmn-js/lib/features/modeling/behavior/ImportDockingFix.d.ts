/**
 * Fix broken dockings after DI imports.
 *
 * @param {EventBus} eventBus
 */
declare function ImportDockingFix(eventBus: any): void;
declare namespace ImportDockingFix {
    const $inject: string[];
}
export default ImportDockingFix;
